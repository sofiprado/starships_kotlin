<resources>

    <!-- Base application theme.-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/starship_list_label</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="starships_list" parent="AppTheme">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/starship_list_value</item>
    </style>

        <style name="starships_list.labels">
            <item name="android:textColor">@color/starship_list_label</item>
        </style>

</resources>
